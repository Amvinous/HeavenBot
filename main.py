import argparse

import bot

if __name__ == '__main__':
    """
    parser = argparse.ArgumentParser()
    parser.add_argument('-sync', action='store_true', default=False)
    args = parser.parse_args()
    bot.run_bot(args.sync)
    """
    bot.run_bot()
